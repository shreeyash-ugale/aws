# Copy this file to .env and fill in the values. Do NOT commit your real secrets.

# App
PORT=8080

# AWS
AWS_REGION=us-east-1
S3_BUCKET=your-securevault-bucket
KMS_KEY_ID=your-kms-key-id

# Admin token for /admin endpoints (choose a strong random value)
ADMIN_TOKEN=change-me-please

# DynamoDB metadata table (created by Terraform). Leave empty to disable metadata features.
DDB_TABLE=securefile_metadata

# Optional: if you prefer static credentials (not recommended for production), uncomment below.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
# AWS_PROFILE=default